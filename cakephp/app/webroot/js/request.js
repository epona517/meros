/**
 * request javascript
 */
var common = common || {};
var request = request || {};

request.init = function () {
	// PROCESS
	// ======================================================================

	// EVENT
	// ======================================================================
	// 【】
	$('#').click(function() {

		common.submit('');
	});
};